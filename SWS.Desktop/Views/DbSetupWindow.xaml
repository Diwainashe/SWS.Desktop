<Window x:Class="SWS.Desktop.Views.DbSetupWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Database Setup"
        Height="320"
        Width="520"
        WindowStartupLocation="CenterScreen">

    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="SQL Server Connection"
                   FontSize="16"
                   FontWeight="SemiBold"/>

        <StackPanel Grid.Row="1" Margin="0,12,0,0">
            <TextBlock Text="Server (e.g. .\SQLEXPRESS or (localdb)\MSSQLLocalDB)"/>
            <TextBox x:Name="ServerBox" Height="28" Margin="0,6,0,0"/>
        </StackPanel>

        <StackPanel Grid.Row="2" Margin="0,12,0,0">
            <TextBlock Text="Database name"/>
            <TextBox x:Name="DatabaseBox" Height="28" Margin="0,6,0,0"/>
        </StackPanel>

        <CheckBox x:Name="WindowsAuthBox"
                  Grid.Row="3"
                  Margin="0,12,0,0"
                  Content="Use Windows Authentication (recommended)"
                  IsChecked="True"
                  Checked="WindowsAuthChanged"
                  Unchecked="WindowsAuthChanged"/>

        <StackPanel Grid.Row="4"
                    Margin="0,12,0,0"
                    x:Name="SqlAuthPanel"
                    Visibility="Collapsed">
            <TextBlock Text="SQL Username"/>
            <TextBox x:Name="UserBox" Height="28" Margin="0,6,0,0"/>

            <TextBlock Text="SQL Password" Margin="0,10,0,0"/>
            <PasswordBox x:Name="PasswordBox" Height="28" Margin="0,6,0,0"/>
        </StackPanel>

        <StackPanel Grid.Row="5"
                    Orientation="Horizontal"
                    HorizontalAlignment="Right"
                    Margin="0,12,0,0">
            <Button Content="Test" Width="90" Margin="0,0,8,0" Click="Test_Click"/>
            <Button Content="Save &amp; Initialize" Width="150" Margin="0,0,8,0" Click="SaveInit_Click"/>
            <Button Content="Cancel" Width="90" Click="Cancel_Click"/>
        </StackPanel>
    </Grid>
</Window>
